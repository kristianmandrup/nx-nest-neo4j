{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxNestUserGenerator",
  "title": "Nx NestJS User Options Schema",
  "description": "Nx NestJS User Options Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the resource.",
      "type": "string",
      "default": "user",
      "x-prompt": "What name would you like to use for the resource?"
    },
    "directory": {
      "description": "The directory of the resource.",
      "type": "string",
      "default": "user"      
    }
  },
  "additionalProperties": false,
  "required": []
}
