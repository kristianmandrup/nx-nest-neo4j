{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxNestAuthGenerator",
  "title": "Nx NestJS Auth Options Schema",
  "description": "Nx NestJS Auth Options Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the resource.",
      "type": "string",
      "default": "auth",
      "x-prompt": "What name would you like to use for the resource?"
    },
    "directory": {
      "description": "The directory of the resource.",
      "type": "string",
      "default": "auth"      
    }
  },
  "additionalProperties": false,
  "required": []
}
