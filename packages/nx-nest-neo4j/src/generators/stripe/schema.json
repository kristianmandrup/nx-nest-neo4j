{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxNestStripeGenerator",
  "title": "Nx NestJS Stripe Options Schema",
  "description": "Nx NestJS Stripe Options Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the resource.",
      "type": "string",
      "default": "stripe",
      "x-prompt": "What name would you like to use for the resource?"
    },
    "directory": {
      "description": "The directory of the resource.",
      "type": "string",
      "default": "stripe"      
    }
  },
  "additionalProperties": false,
  "required": []
}
